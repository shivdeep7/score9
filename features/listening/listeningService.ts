
import { QuestionsListNameType } from "./listeningSlice";

// Create a serivce to get the list of the summary questions 
export interface QuestionsPropsTypes {
    uri: string
    name: QuestionsListNameType
}
